(** A library providing core building blocks for creating NATS clients. *)

module Errors = Errors
module Message = Message
module Protocol = Protocol
module Parse = Parse
module Serialize = Serialize
